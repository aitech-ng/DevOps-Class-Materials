apiVersion: apps/v1
kind: Deployment
metadata:
  name: myreactapp-deployment
  labels:
    app: react
spec:
  selector:
    matchLabels:
      app: react
  replicas: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
      - name: reactapp-container
        image: gerrome/staticwebsite
        imagePullPolicy: Always
        ports:
        - containerPort: 80